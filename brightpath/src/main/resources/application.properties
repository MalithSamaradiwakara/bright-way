# Application name
spring.application.name=brightway_lms

# Database connection
spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/railway
spring.datasource.username=root
spring.datasource.password=eZlylTrNSflszevNHTWObZThRtxSiIDS
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA and Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# File upload directory (Optional: adjust or externalize in production)
file.upload-dir=/app/uploads

# Optional: Server port or additional configs
# server.port=8080
